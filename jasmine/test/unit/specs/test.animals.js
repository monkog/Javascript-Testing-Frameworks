const { dog, cat } = require("../../../../src/animals");

describe('', () => {
    it('', () => {
        expect(dog()).toBe("dog");
    })
    
    it('', () => {
        expect(cat()).toBe("cat");
    })
})